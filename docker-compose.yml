version: '3'

services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    environment:
      SECRET_KEY: ddssdsd
    volumes:
      - .:/code:delegated
    ports:
      - "8000:8000"
